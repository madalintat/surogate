name: Build Wheels

on:
  push:
    tags:
      - 'v*'

jobs:
  wheel:
    name: Build wheel (CUDA ${{ matrix.cuda_version }})
    runs-on: self-hosted
    container:
      image: ${{ matrix.cuda_build_image }}
      options: -v /var/cache/surogate-ccache/${{ matrix.cuda_version }}:/ccache

    strategy:
      matrix:
        include:
          - cuda_version: "12.8"
            cuda_build_image: "nvidia/cuda:12.8.1-cudnn-devel-ubuntu24.04"
            wheel_tag: "cu128"
            archs: "80;86;89;90;100a;120a"
          - cuda_version: "12.9"
            cuda_build_image: "nvidia/cuda:12.9.1-cudnn-devel-ubuntu24.04"
            wheel_tag: "cu129"
            archs: "80;86;89;90;100a;103a;120a"
          - cuda_version: "13.1"
            cuda_build_image: "nvidia/cuda:13.1.1-cudnn-devel-ubuntu24.04"
            wheel_tag: "cu130"
            archs: "80;86;89;90;100a;103a;120a"
    env:
      CMAKE_GENERATOR: Ninja
      CMAKE_C_COMPILER: gcc-13
      CMAKE_CXX_COMPILER: g++-13
      CMAKE_C_COMPILER_LAUNCHER: ccache
      CMAKE_CXX_COMPILER_LAUNCHER: ccache
      CMAKE_CUDA_COMPILER_LAUNCHER: ccache
      CMAKE_BUILD_PARALLEL_LEVEL: 8
      CUDAARCHS: ${{ matrix.archs }}
      UV_NO_SYNC: 1
      CCACHE_DIR: /ccache
      CCACHE_MAXSIZE: 10G
      CCACHE_CUDA_PATHS: /usr/local/cuda

    defaults:
      run:
        working-directory: ${{ github.workspace }}

    steps:
      - name: Install System Dependencies
        run: |
          apt-get update && apt-get install -y --no-install-recommends --allow-change-held-packages \
          git wget build-essential cmake ninja-build python3-dev ccache libopenmpi-dev libdw-dev libelf-dev libnccl2 libnccl-dev \
          g++-13

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          fetch-tags: true

      - name: Mark workspace as safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup ccache
        run: |
          mkdir -p /ccache
          ccache --max-size=10G
          ccache --zero-stats

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Add CUDA version tag
        run: uv run --no-project --with tomlkit python3 .github/scripts/set_cuda_version_tag.py "${{ matrix.wheel_tag }}"

      - name: Build wheel
        run: uv build --wheel --out-dir wheelhouse

      - name: Show ccache stats
        run: ccache --show-stats

      - name: Repair wheel
        run: |
          uv run --no-project --with auditwheel --with patchelf auditwheel repair wheelhouse/*.whl \
            -w repaired/ \
            --exclude libcuda.so.1 \
            --exclude libcudart.so.12 \
            --exclude libcudart.so.13 \
            --exclude libcudnn.so.9 \
            --exclude libcufile.so.0 \
            --exclude libnccl.so.2 \
            --exclude libcublas.so.12 \
            --exclude libcublas.so.13 \
            --exclude libcublasLt.so.12 \
            --exclude libcublasLt.so.13 \
            --exclude libnvidia-ml.so.1
          mv repaired/*.whl wheelhouse/
          rm -f wheelhouse/*linux_x86_64*.whl
          ls -lh wheelhouse/

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.wheel_tag }}
          path: wheelhouse/*.whl

  release:
    needs: wheel
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          fetch-tags: true

      - name: Find previous tag
        id: prev
        shell: bash
        run: |
          set -euo pipefail
          current="${GITHUB_REF_NAME}"

          # previous tag before the current one (works well for semver-like vX.Y.Z tags)
          prev="$(git describe --tags --abbrev=0 "${current}^" 2>/dev/null || true)"

          echo "current=$current" >> "$GITHUB_OUTPUT"
          echo "prev=$prev" >> "$GITHUB_OUTPUT"
      - name: Generate release notes from Conventional Commits
        uses: orhun/git-cliff-action@v4
        with:
          args: >-
            ${{ steps.prev.outputs.prev && format('{0}..HEAD', steps.prev.outputs.prev) || '' }}
            --strip header
        env:
          OUTPUT: release-notes.md
          GITHUB_REPO: ${{ github.repository }}
      - name: Download all wheels
        uses: actions/download-artifact@v4
        with:
          pattern: wheel-*
          path: wheelhouse/
          merge-multiple: true

      - name: Create release with changelog
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          body_path: release-notes.md
          files: wheelhouse/*.whl
          draft: false
          fail_on_unmatched_files: true
